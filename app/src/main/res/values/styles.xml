<resources>

    <!-- Custom Bottom Navigation Text Style -->
    <style name="BottomNavTextSmall" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Welcome Text Style -->
    <style name="WelcomeText">
        <item name="android:textSize">40sp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Subtitle Text Style -->
    <style name="SubtitleText">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">#757575</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <!-- Custom App Theme based on Material Components -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/black</item>

        <item name="colorSecondary">@color/blue</item>
        <item name="colorSecondaryVariant">@color/blue_dark</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Customize the Status Bar and Navigation Bar -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowFullscreen">true</item>

        <!-- Optional: Set specific Material button styles if needed -->
        <item name="buttonStyle">@style/Widget.MaterialComponents.Button</item>
    </style>

</resources>
